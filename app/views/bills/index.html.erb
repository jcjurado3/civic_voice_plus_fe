<%= button_to "Search Bills", bills_path, method: :get %>
<br />
<%= form_with url: bills_path, method: :get, local: true do |f| %>
  <%= f.label :search, "Search by topic" %>
  <%= f.text_field :search %>
  <%= f.label :state, "Select region" %>
  <%= f.select :state, options_for_select(us_states, '') %>

  <%= f.submit 'Search' %>
<% end %>
<br /><br /> 

<% if !params[:search].nil? %>
  <center><b style="border: solid black; padding:50px; float:left">
    <b style="font-size:30px;">Search Results</b>
    <% @results.drop(1).each do |result| %>
      <p>Bill Name: <%= link_to "#{result[1][:bill_number]}", bill_path(result[1][:bill_id]) %></p>
      <p>Description: <%= result[1][:title] %></p>
      <%= form_with url: user_bills_path, method: :post, local: true do |f| %>
        <%= f.hidden_field :user_id, value: @user.id %>
        <%= f.hidden_field :bill_id, value: "#{result[1][:bill_id]}" %>
        <%= f.submit "save #{result[1][:bill_number]} to my dashboard" %>
      <% end %>
    <% end %>
  </b>
<% end %>